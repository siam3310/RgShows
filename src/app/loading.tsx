import React from "react";

const loading = () => {
  return (
    <div className="flex justify-center items-center h-screen w-screen overflow-hidden">
      <span className="pageLoader font-semibold overflow-hidden">Rg Shows</span>
    </div>
  );
};

export default loading;
